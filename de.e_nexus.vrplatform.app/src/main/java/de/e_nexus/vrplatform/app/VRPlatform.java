package de.e_nexus.vrplatform.app;

/** Runs framework standalone: a VR session showing just the skydome. */
public class VRPlatform {
    public static void main(String[] args) {
        new VRPlatformApplication().run();
    }
}
